const mysql = require('mysql');
var connection;
  connection = {
    host: 'localhost',
    user: 'root',
    password: '1234',
    database: 'adiuas',
    port: 3306
  };

module.exports = connection;
